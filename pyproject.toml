[project]
name = "aarch3-an-arm64-dynamic-instrumentation-framework-for-re3"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "amd1.7"}
]
requires-python = ">=3.9"
dependencies = [
    "loguru>=0.7.3",
    "pybind11>=3.1.0",
    "python-dotenv>=1.2.1",
]

[project.scripts]
aarch3-an-arm64-dynamic-instrumentation-framework-for-re3 = "aarch3_an_arm64_dynamic_instrumentation_framework_for_re3:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pybind11-stubgen>=2.5.5",
]
